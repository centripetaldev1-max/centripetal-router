# Copy this file to .env.local and fill in real values.
# NEVER commit .env.local or any file containing real secrets.

# Optional: Supabase connection (not used in current service logic)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Router mode: set to "true" to enable smart routing placeholder
ROUTER_SMART_ENABLED=false
